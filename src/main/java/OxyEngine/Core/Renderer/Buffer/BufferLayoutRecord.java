package OxyEngine.Core.Renderer.Buffer;

public record BufferLayoutRecord(VertexBuffer vertexBuffer, IndexBuffer indexBuffer, TextureBuffer textureBuffer,
                                 TangentBuffer tangentBuffer, NormalsBuffer normalsBuffer) {
}
